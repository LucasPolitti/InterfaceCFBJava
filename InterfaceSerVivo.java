public interface InterfaceSerVivo {

    // public boolean vivo = true; //Esse atributo não precisa ser implementado porque já foi adicionado um valor a ele aqui, na interface. No entanto ele também não poderia ser mudado na classe, daria erro. Por isso não é usal implementar atributos na interface.

    public void mover();
    public void comer(int massa);
    public void info();


}